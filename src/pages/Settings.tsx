import { FC } from "react";

const Settings: FC = () => {
    return (
        <div className="p-4">
            <h1 className="text-2xl font-bold">Settings</h1>
        </div>
    );
};

export default Settings;
